# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: zava-storefront
metadata:
  template: zava-storefront@0.0.1-beta

variables:
  AZURE_ENV_NAME_MAX_LENGTH: 64

infra:
  provider: bicep
  path: infra

services:
  web:
    project: ./src
    language: dotnet
    host: appservice